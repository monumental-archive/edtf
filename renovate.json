{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["github>monumental-archive/.github#v1.60.0"],
  "packageRules": [
    {
      "description": "pgrx moves in lockstep: the crate deps plus the cargo-pgrx build input pinned in mise.toml, which must equal them",
      "matchPackageNames": ["pgrx", "pgrx-tests", "cargo-pgrx"],
      "groupName": "pgrx"
    }
  ]
}
